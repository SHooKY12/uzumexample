<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <TextView

            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="for xzczxcxzczxcxzcxzcxz fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 age content is ce for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout: for fragment code we just setup our recyclerview with sample data and adapter.

For SampleFragment2 we don't want to use recyclerview but Let's see we want to use simple scrollable content. in that case you will need Nested Scrollable with layout behavior property.

below is the sample layout:nter aligned. but when its expanded/ collapse state content doesn't look center aligned when expanded state haven't found proper solution to have message center aligned in both expand/collapse state."
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.core.widget.NestedScrollView>
